version: '3.9'

services:
  web:
    container_name: "mintos-currency-select"
    build:
      context: .
      dockerfile: app.Dockerfile
    volumes:
      - type: bind
        source: ./
        target: /web/app
    ports:
      - "8000:80"
    tty: true
